import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export type RecoveredPaymentRecord = { id: string; source: string; occurred_at: string | null; amount: number | null; currency: string | null; payment_status: string | null; customer_name: string | null; customer_email: string | null; customer_phone: string | null; payment_reference: string | null }

export function RecoveredPaymentSection({ title, records }: { title: string; records: RecoveredPaymentRecord[] }) {
  return <Card><CardHeader><CardTitle>{title} ({records.length})</CardTitle></CardHeader><CardContent><p className="mb-4 text-sm text-muted-foreground">Payment-verified recovery records. Missing booking or application fields remain unrecovered until the original data source is restored.</p>{records.length === 0 ? <p className="py-4 text-sm text-muted-foreground">No recoverable records are available from the current exports.</p> : <div className="overflow-x-auto"><table className="w-full min-w-[760px] text-sm"><thead><tr className="border-b border-border"><th className="px-2 py-3 text-left font-medium text-muted-foreground">Contact</th><th className="px-2 py-3 text-left font-medium text-muted-foreground">Email</th><th className="px-2 py-3 text-left font-medium text-muted-foreground">Phone</th><th className="px-2 py-3 text-right font-medium text-muted-foreground">Amount</th><th className="px-2 py-3 text-left font-medium text-muted-foreground">Status</th><th className="px-2 py-3 text-left font-medium text-muted-foreground">Source</th><th className="px-2 py-3 text-left font-medium text-muted-foreground">Date</th></tr></thead><tbody>{records.map((record) => <tr key={record.id} className="border-b border-border last:border-0"><td className="px-2 py-3 font-medium">{record.customer_name || "Unknown"}</td><td className="px-2 py-3 text-muted-foreground">{record.customer_email || "-"}</td><td className="px-2 py-3 text-muted-foreground">{record.customer_phone || "Not recovered"}</td><td className="px-2 py-3 text-right font-medium">{record.amount == null ? "-" : new Intl.NumberFormat("en-GB", { style: "currency", currency: record.currency || "GBP" }).format(record.amount)}</td><td className="px-2 py-3 text-muted-foreground">Confirmed (recovered)</td><td className="px-2 py-3 text-muted-foreground">{record.source === "donation_manager" ? "Donation Manager" : "Stripe"}</td><td className="px-2 py-3 text-muted-foreground">{record.occurred_at ? new Date(record.occurred_at).toLocaleDateString("en-GB") : "Unknown"}</td></tr>)}</tbody></table></div>}</CardContent></Card>
}
