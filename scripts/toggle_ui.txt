
                  <div className="flex gap-2 mb-4">
                    <button
                      type="button"
                      onClick={() => update("donation_type", "one-off")}
                      className={`flex-1 rounded-xl border-2 p-3 text-center text-sm font-semibold transition-all ${form.donation_type === "one-off" ? "border-primary bg-primary/5 shadow-md" : "border-muted hover:border-primary/40"}`}
                    >
                      One-off payment
                    </button>
                    <button
                      type="button"
                      onClick={() => update("donation_type", "recurring")}
                      className={`flex-1 rounded-xl border-2 p-3 text-center text-sm font-semibold transition-all ${form.donation_type === "recurring" ? "border-primary bg-primary/5 shadow-md" : "border-muted hover:border-primary/40"}`}
                    >
                      Monthly subscription
                    </button>
                  </div>

                  {form.donation_type === "recurring" && (
                    <p className="text-xs text-muted-foreground text-center mb-3">
                      A monthly payment of your chosen amount will be set up via Stripe.
                    </p>
                  )}
